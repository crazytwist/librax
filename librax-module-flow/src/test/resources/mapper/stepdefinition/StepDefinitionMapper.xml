<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.librax.lab.module.flow.dal.mysql.stepdefinition.StepDefinitionMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->

    <select id="selectLatestActiveByKeys"
            resultType="com.librax.lab.module.flow.dal.dataobject.stepdefinition.StepDefinitionDO">

        SELECT sd.*
        FROM pd_step_definition sd
        INNER JOIN (
        SELECT
        step_key,
        MAX(version) AS max_version
        FROM pd_step_definition
        WHERE
        step_key IN
        <foreach collection="stepKeys" item="key" open="(" separator="," close=")">
            #{key}
        </foreach>
        AND status = 'ACTIVE'
        AND deleted = 0
        GROUP BY step_key
        ) latest
        ON sd.step_key = latest.step_key
        AND sd.version = latest.max_version
        WHERE sd.deleted = 0

    </select>

    <select id="selectByKeyAndVersion"
            resultType="com.librax.lab.module.flow.dal.dataobject.stepdefinition.StepDefinitionDO">

        SELECT
        *
        FROM
        pd_step_definition
        WHERE
        step_key = #{stepKey}
        AND version = #{version}
        AND deleted = 0
        LIMIT
        1

    </select>
</mapper>