<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>librax</artifactId>
        <groupId>com.librax.boot</groupId>
        <version>${revision}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>librax-module-mall</artifactId>
    <packaging>pom</packaging>

    <name>${project.artifactId}</name>

    <description>
        商城大模块，由 product 商品、promotion 营销、trade 交易、statistics 统计等组成
    </description>
    <modules>
        <module>librax-module-product</module>
        <module>librax-module-promotion</module>
        <module>librax-module-trade</module>
        <module>librax-module-statistics</module>
        <!--
            特殊：为什么会有 librax-module-trade-api 呢？
                librax-module-promotion 和 librax-module-trade 之间相互循环依赖，所以抽出 librax-module-trade-api 模块，这样：
                1. librax-module-promotion 依赖 librax-module-trade-api
                2. librax-module-trade 依赖 librax-module-promotion
            从而不存在相互（循环）依赖，即 librax-module-trade => librax-module-promotion => librax-module-trade-api
         -->
        <module>librax-module-trade-api</module>
    </modules>

</project>
